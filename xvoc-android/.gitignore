*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.keystore
release.keystore

# Wrapper jar/scripts are generated by CI (or by Android Studio on first
# open) rather than committed, since this project was authored without
# network access to fetch a real gradle-wrapper.jar binary.
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar
